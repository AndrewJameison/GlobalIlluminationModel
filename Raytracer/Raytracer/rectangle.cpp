#include "rectangle.hpp"

float Rectangle::Intersect(Ray ray)
{
    // TODO: hold a list of triangles to make the rectangle
        // Eventually create a polygon class, feeding in datapoints instead of mathing out their positions
    return 0.0f;
}
